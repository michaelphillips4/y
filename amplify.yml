version: 1
frontend:
  phases:
    build:
      commands:
        - echo build started
        - cd UI/ReactVite
        - echo pwd
        - npm i
        - npm run build
  artifacts:
    baseDirectory: /UI/ReactVite/dist
    files:
      - '**/*'