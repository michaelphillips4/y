version: 1
frontend:
  phases:
    build:
      commands:
        - echo build started
        - cd UI
        - echo pwd
        - npm i
        - npm run build
  artifacts:
    baseDirectory: /UI/dist
    files:
      - '**/*'