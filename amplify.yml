version: 1
frontend:
  phases:
    build:
      commands:
        - cd UI/ReactVite
        - npm install
        - npm run build
  artifacts:
    baseDirectory: /UI/ReactVite/dist
    files:
      - '**/*'